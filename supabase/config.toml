# Substitui o id após `npx supabase link --project-ref <teu-ref>` (local).
# Não commitar aqui um project ref real se o repositório for público/partilhado.
[project]
id = "aaaaaaaaaaaaaaaaaaaa"

[api]
port = 54321
schemas = ["public"]

[db]
port = 54322

[studio]
port = 54323

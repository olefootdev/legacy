// Stub no-op: seção de mentoria legacy no perfil de jogador. Renderiza nada.

import type { PlayerEntity } from '@/entities/types';

export function LegacyMentorSection(_props: { student: PlayerEntity }): null {
  return null;
}

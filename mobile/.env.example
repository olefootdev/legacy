# Copiar para `mobile/.env` (ignorado pelo Git). Variáveis EXPO_PUBLIC_* vão para o bundle.
#
# URL do viewer Babylon (dev: máquina que corre web/match-pitch, porta típica 5174).
EXPO_PUBLIC_PITCH_URL=
# URL do jogo web manager (opcional — partidas no browser).
EXPO_PUBLIC_OLEFOOT_WEB_URL=

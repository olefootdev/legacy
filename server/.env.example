# Copiar para `server/.env`. Nunca commitar `server/.env` com valores reais.
#
# ─── Supabase (servidor) ─────────────────────────────────────────────
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Service role: só no servidor — nunca no frontend nem em repo público.
SUPABASE_SERVICE_ROLE_KEY=

# ─── Opcional ────────────────────────────────────────────────────────
# DATABASE_URL=
PORT=
CORS_ORIGIN=

# ─── OpenAI (POST /api/game-spirit/teach) ─────────────────────────────
OPENAI_API_KEY=
# OPENAI_MODEL=
